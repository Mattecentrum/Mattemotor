/*'use strict';

describe('Service: exerciseList', function () {

  // load the service's module
  beforeEach(module('mattemotorApp'));

  // instantiate service
  var exerciseList;
  beforeEach(inject(function (_exerciseList_) {
    exerciseList = _exerciseList_;
  }));

  it('Should return 5 exercises in list', function () {
    console.log("Exercises",exerciseList);
    exerciseList.Load(1, function(data) {
      expect(data.exercises.length).toBe(5);
    });
  });

});*/
